<views:MvxWindowsContentDialog
    xmlns:views="using:MvvmCross.Platforms.Uap.Views"
    x:Class="SigfolioWallet.Views.Modal.PasswordDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="400" Height="170"
    Background="{ThemeResource SystemControlHighlightAccentBrush}" BorderBrush="{ThemeResource SystemColorHighlightBrush }" BorderThickness="1">
    <StackPanel Margin="10">
        <TextBlock Text="Password Dialog" Margin="0 0 0 20" />
        <TextBlock Margin="10 0 0 0" Text="Password"/>
        <TextBox Margin="10" Text="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="0 0 10 0">
            <Button Margin="6" Command="{x:Bind ViewModel.Confirm}">Confirm</Button>
            <Button Command="{x:Bind ViewModel.Cancel}">Cancel</Button>
        </StackPanel>
    </StackPanel>
</views:MvxWindowsContentDialog>
